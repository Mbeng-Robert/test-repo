name: testing workflow

push:
     branches:
         -main

workflow_dispatch:


jobs:
  
    myfirstjob:
         runs-on: ubuntu-latest
         steps:
            -name: clone my repo
             uses: actions/checkout@v4
      

